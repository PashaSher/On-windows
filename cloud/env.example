# См. ice-config-server.env.example — тот же формат для TURN / ICE_CONFIG_TOKEN / хост / порт.
# Ручной запуск на Linux: ./scripts/ice-config-server.sh (подхватывает cloud/ice-config-server.env).

TURN_URLS=turn:turn.example.com:3478?transport=udp,turns:turn.example.com:5349?transport=tcp
TURN_USERNAME=romeo
TURN_PASSWORD=change-me
ICE_CONFIG_TOKEN=
